---
import { Code, Heart, Megaphone, ArrowRight, Users, Zap, CheckCircle } from "lucide-astro";
import teams from "@data/teams";

const adoptableTeams = teams.filter(t => t.isExample);
---

<section id="cta" class="relative py-16 sm:py-20 md:py-24 px-4 sm:px-6 md:px-8 overflow-hidden">
  <!-- Dark background with grid pattern -->
  <div class="absolute inset-0 bg-neutral-900"></div>
  <div class="absolute inset-0 opacity-10 grid-pattern"></div>

  <!-- Subtle animated background -->
  <div class="absolute inset-0 bg-gradient-to-br from-orange-500/5 via-neutral-500/5 to-orange-500/5 animate-gradient-shift"></div>

  <div class="relative z-10 max-w-7xl mx-auto">
    <!-- Urgency Banner -->
    <div class="flex justify-center mb-8">
      <div class="inline-flex items-center gap-3 px-5 py-3 rounded-full bg-orange-500/20 border border-orange-500/40">
        <span class="flex h-2.5 w-2.5">
          <span class="animate-ping absolute inline-flex h-2.5 w-2.5 rounded-full bg-orange-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-orange-500"></span>
        </span>
        <span class="text-sm sm:text-base font-bold text-orange-300">{adoptableTeams.length} Teams Available for Adoption</span>
      </div>
    </div>

    <!-- Header -->
    <div class="text-center mb-10 sm:mb-12 md:mb-16 cta-header">
      <h2 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-black mb-4 sm:mb-6 text-white tracking-tight leading-tight">
        Ready to join?<br/><span class="text-orange-500">Claim your team now.</span>
      </h2>
      <div class="max-w-3xl mx-auto space-y-2 sm:space-y-3">
        <p class="text-base sm:text-lg md:text-xl lg:text-2xl text-neutral-300 leading-relaxed">
          15 days. Real problems. Real impact. <span class="text-orange-400 font-bold">$5K+ in prizes.</span>
        </p>
        <p class="text-base sm:text-lg md:text-xl text-neutral-400">
          Skip the brainstorming — adopt a pre-built team idea and start hacking immediately.
        </p>
      </div>
    </div>

    <!-- Three CTA Tracks -->
    <div class="grid sm:grid-cols-2 md:grid-cols-3 gap-4 sm:gap-6 md:gap-8 mb-10 sm:mb-12 md:mb-16">
      <!-- Register Your Team - Primary CTA -->
      <div class="cta-card group relative sm:col-span-2 md:col-span-1">
        <div class="absolute -inset-px bg-gradient-to-b from-orange-500/50 to-orange-500/0 rounded-xl sm:rounded-2xl"></div>
        <div class="relative bg-neutral-800 rounded-xl sm:rounded-2xl p-5 sm:p-6 md:p-8 h-full flex flex-col border-t-4 border-orange-500">
          <!-- Badge -->
          <div class="absolute -top-3 left-6 px-3 py-1 bg-orange-500 text-white text-xs font-bold rounded-full">
            Most Popular
          </div>

          <!-- Icon -->
          <div class="w-12 h-12 sm:w-14 sm:h-14 md:w-16 md:h-16 mb-4 sm:mb-5 md:mb-6 rounded-xl bg-orange-500/20 flex items-center justify-center text-orange-400 group-hover:scale-110 transition-transform duration-300">
            <Users size={24} class="sm:size-7 md:size-8" strokeWidth={2.5} />
          </div>

          <!-- Title -->
          <h3 class="text-xl sm:text-2xl font-bold text-white mb-4 sm:mb-5 md:mb-6">Register Your Team</h3>

          <!-- Benefits -->
          <div class="space-y-2 sm:space-y-3 mb-5 sm:mb-6 md:mb-8 flex-grow">
            <div class="flex items-start gap-2 sm:gap-3">
              <CheckCircle size={16} class="text-green-400 mt-0.5 shrink-0" />
              <p class="text-sm sm:text-base text-neutral-300">Adopt a pre-built project idea</p>
            </div>
            <div class="flex items-start gap-2 sm:gap-3">
              <CheckCircle size={16} class="text-green-400 mt-0.5 shrink-0" />
              <p class="text-sm sm:text-base text-neutral-300">Solo or team (any size)</p>
            </div>
            <div class="flex items-start gap-2 sm:gap-3">
              <CheckCircle size={16} class="text-green-400 mt-0.5 shrink-0" />
              <p class="text-sm sm:text-base text-neutral-300 font-bold">Start building today</p>
            </div>
          </div>

          <!-- Button -->
          <a href="/join" class="cta-button cta-button-primary group/btn relative overflow-hidden rounded-xl py-3 sm:py-3.5 md:py-4 px-5 sm:px-6 font-bold text-center transition-all duration-300 active:scale-95">
            <div class="absolute inset-0 bg-orange-500 group-hover/btn:bg-orange-400 transition-colors"></div>
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover/btn:translate-x-full transition-transform duration-700"></div>
            <span class="relative z-10 flex items-center justify-center gap-2 text-white text-base sm:text-lg">
              Register Now — Free
              <ArrowRight size={18} class="sm:size-5 group-hover/btn:translate-x-1 transition-transform duration-300" />
            </span>
          </a>
        </div>
      </div>

      <!-- For Sponsors -->
      <div class="cta-card group relative">
        <div class="absolute -inset-px bg-gradient-to-b from-orange-500/50 to-orange-500/0 rounded-xl sm:rounded-2xl"></div>
        <div class="relative bg-neutral-800 rounded-xl sm:rounded-2xl p-5 sm:p-6 md:p-8 h-full flex flex-col border-t-4 border-orange-500">
          <!-- Icon -->
          <div class="w-12 h-12 sm:w-14 sm:h-14 md:w-16 md:h-16 mb-4 sm:mb-5 md:mb-6 rounded-xl bg-orange-500/20 flex items-center justify-center text-orange-400 group-hover:scale-110 transition-transform duration-300">
            <Heart size={24} class="sm:size-7 md:size-8" strokeWidth={2.5} />
          </div>

          <!-- Title -->
          <h3 class="text-xl sm:text-2xl font-bold text-white mb-4 sm:mb-5 md:mb-6">Fuel the Mission</h3>

          <!-- Description -->
          <p class="text-sm sm:text-base text-neutral-300 leading-relaxed mb-5 sm:mb-6 md:mb-8 flex-grow">
            Every dollar grows the prize pool. Every dollar goes directly to builders solving real problems.
          </p>

          <!-- Button -->
          <a href="#sponsors" class="cta-button cta-button-secondary group/btn relative overflow-hidden rounded-xl py-3 sm:py-3.5 md:py-4 px-5 sm:px-6 font-bold text-center transition-all duration-300 border-2 border-orange-500 active:scale-95">
            <div class="absolute inset-0 bg-orange-500/0 group-hover/btn:bg-orange-500/10 transition-colors"></div>
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-orange-400/20 to-transparent -translate-x-full group-hover/btn:translate-x-full transition-transform duration-700"></div>
            <span class="relative z-10 flex items-center justify-center gap-2 text-orange-400 group-hover/btn:text-orange-300 text-base sm:text-lg transition-colors">
              Become a Sponsor
              <ArrowRight size={18} class="sm:size-5 group-hover/btn:translate-x-1 transition-transform duration-300" />
            </span>
          </a>
        </div>
      </div>

      <!-- For Supporters -->
      <div class="cta-card group relative">
        <div class="absolute -inset-px bg-gradient-to-b from-neutral-600/50 to-neutral-600/0 rounded-xl sm:rounded-2xl"></div>
        <div class="relative bg-neutral-800 rounded-xl sm:rounded-2xl p-5 sm:p-6 md:p-8 h-full flex flex-col border-t-4 border-neutral-500">
          <!-- Icon -->
          <div class="w-12 h-12 sm:w-14 sm:h-14 md:w-16 md:h-16 mb-4 sm:mb-5 md:mb-6 rounded-xl bg-neutral-600/30 flex items-center justify-center text-neutral-300 group-hover:scale-110 transition-transform duration-300">
            <Megaphone size={24} class="sm:size-7 md:size-8" strokeWidth={2.5} />
          </div>

          <!-- Title -->
          <h3 class="text-xl sm:text-2xl font-bold text-white mb-4 sm:mb-5 md:mb-6">Spread the Word</h3>

          <!-- Description -->
          <p class="text-sm sm:text-base text-neutral-300 leading-relaxed mb-5 sm:mb-6 md:mb-8 flex-grow">
            Can't code? Share with developers. Test prototypes. Connect teams with orgs.
          </p>

          <!-- Button -->
          <a href="https://discord.gg/vibecodeutah" target="_blank" rel="noopener noreferrer" class="cta-button cta-button-tertiary group/btn relative overflow-hidden rounded-xl py-3 sm:py-3.5 md:py-4 px-5 sm:px-6 font-bold text-center transition-all duration-300 border-2 border-neutral-500 active:scale-95">
            <div class="absolute inset-0 bg-neutral-500/0 group-hover/btn:bg-neutral-500/10 transition-colors"></div>
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent -translate-x-full group-hover/btn:translate-x-full transition-transform duration-700"></div>
            <span class="relative z-10 flex items-center justify-center gap-2 text-neutral-300 group-hover/btn:text-white text-base sm:text-lg transition-colors">
              Join Community
              <ArrowRight size={18} class="sm:size-5 group-hover/btn:translate-x-1 transition-transform duration-300" />
            </span>
          </a>
        </div>
      </div>
    </div>

    <!-- Bottom Strip -->
    <div class="text-center space-y-3 sm:space-y-4 bottom-strip">
      <div class="inline-block relative">
        <div class="absolute -inset-2 bg-orange-500/20 blur-lg rounded-lg"></div>
        <p class="relative text-xl sm:text-2xl md:text-3xl lg:text-4xl font-black text-white tracking-tight px-4 sm:px-6 md:px-8 py-3 sm:py-4">
          No pitch deck required. Just register.
        </p>
      </div>
      <a href="/join" class="inline-flex items-center gap-2 text-orange-400 hover:text-orange-300 font-bold transition-colors">
        <span>Register your team now</span>
        <ArrowRight size={18} />
      </a>
      <p class="text-[10px] sm:text-xs uppercase tracking-[0.2em] sm:tracking-[0.3em] text-neutral-500 font-bold">
        QUIET GIANTS — CODE WITH A CONSCIENCE
      </p>
    </div>
  </div>
</section>

<style>
  /* Grid pattern background */
  .grid-pattern {
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
    background-size: 50px 50px;
  }

  /* Gradient animation */
  @keyframes gradient-shift {
    0%, 100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  .animate-gradient-shift {
    background-size: 200% 200%;
    animation: gradient-shift 15s ease infinite;
  }

  /* Staggered fade-in animations */
  .cta-header {
    animation: fadeInUp 0.8s ease-out both;
  }

  .cta-card:nth-child(1) {
    animation: fadeInUp 0.8s ease-out 0.2s both;
  }

  .cta-card:nth-child(2) {
    animation: fadeInUp 0.8s ease-out 0.4s both;
  }

  .cta-card:nth-child(3) {
    animation: fadeInUp 0.8s ease-out 0.6s both;
  }

  .bottom-strip {
    animation: fadeInUp 0.8s ease-out 0.8s both;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Card hover glow effects */
  .cta-card:nth-child(1):hover {
    box-shadow: 0 0 40px rgba(249, 115, 22, 0.3);
  }

  .cta-card:nth-child(2):hover {
    box-shadow: 0 0 40px rgba(249, 115, 22, 0.3);
  }

  .cta-card:nth-child(3):hover {
    box-shadow: 0 0 40px rgba(115, 115, 115, 0.3);
  }

  /* Button hover glow effects */
  .cta-button-primary:hover {
    box-shadow: 0 0 30px rgba(249, 115, 22, 0.5);
  }

  .cta-button-secondary:hover {
    box-shadow: 0 0 30px rgba(249, 115, 22, 0.5);
  }

  .cta-button-tertiary:hover {
    box-shadow: 0 0 30px rgba(115, 115, 115, 0.3);
  }

  /* Smooth transitions */
  .cta-card {
    transition: all 0.3s ease;
  }

  .cta-button {
    transition: all 0.3s ease;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .cta-header h2 {
      font-size: 2.5rem;
      line-height: 1.2;
    }
  }
</style>

<script>
  // GSAP scroll-triggered animations (if GSAP is installed)
  // This is optional and will enhance the experience if GSAP is available
  if (typeof window !== 'undefined') {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
          }
        });
      },
      {
        threshold: 0.1,
        rootMargin: '0px 0px -100px 0px',
      }
    );

    // Observe all CTA cards
    document.querySelectorAll('.cta-card').forEach((card) => {
      observer.observe(card);
    });
  }
</script>
